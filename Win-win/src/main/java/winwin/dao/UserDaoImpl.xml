<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="winwin.dao.UserDao">
  
   <insert id="join" parameterType="winwin.dto.User">
		INSERT INTO member ( userid, username, phone, pwd ) VALUES ( #{userid }, #{username }, #{phone }, #{pwd } )
	</insert>
	
	<select id="selectCnt" parameterType="winwin.dto.User" resultType="int" >
		select count(*) from member where userid=#{userid } and pwd=#{pwd }
	</select>
	
	<select id="select" resultType="winwin.dto.User">
		select * from member where userid=#{userid }
	</select>
	
	<select id="idcheck" parameterType="winwin.dto.User" resultType="int" >
		select count(*) from member where userid=#{userid }
	</select>
  </mapper>