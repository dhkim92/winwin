<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="winwin.dao.JobopenDao">
  
  	<insert id="insertBasic" parameterType="winwin.dto.JobopenBasic">
  		<selectKey order="BEFORE" keyProperty="jobopenNo" resultType="int">
  			select JobOpenBasic_seq.nextval from dual
  		</selectKey>
  		insert into jobopenBasic(jobopenNo, title, form, startPay, endPay, rulePay, startDate, endDate, allOpen)
  		values(#{jobopenNo}, #{title}, #{form}, #{startPay}, #{endPay}, #{rulePay},
  		<if test="startDate!=null">
  		#{startDate}, 
  		</if> 
  		<if test="startDate==null">
  		null, 
  		</if> 
 		<if test="endDate!=null">
  		#{endDate}, 
  		</if> 
  		<if test="endDate==null">
  		null, 
  		</if> 
   		 #{allOpen})
  	</insert>
  
  </mapper>