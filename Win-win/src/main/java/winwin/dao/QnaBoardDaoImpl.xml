<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="winwin.dao.QnaBoardDao">
 	<select id="list" parameterType="winwin.util.Paging" resultType="winwin.dto.QnaBoard">
 		SELECT
 			B.qnaNo,
 			B.title,
 			B.userId,
 			B.writer,
 			B.pw,
 			B.content,
 			B.hit,
 			B.writedate,
 			B.asw_code,
 			B.asw_writer,
 			B.asw_content,
 			B.asw_date
 		FROM (
 			SELECT rownum rnum, A.*
 			FROM(
 				SELECT * FROM qna
 				ORDER BY qnano DESC
 			)A
 			ORDER BY rnum
 		)B
 		WHERE B.rnum BETWEEN #{startNo} AND #{endNo}
 	</select>
 	<select id="totalCnt" resultType="int">
 		SELECT count(*)
 		FROM qna
 	</select>
 	<select id="view" parameterType="winwin.dto.QnaBoard" resultType="winwin.dto.QnaBoard">
 		SELECT
 			qnaNo,
 			title,
 			userId,
 			writer,
 			pw,
 			content,
 			hit,
 			writedate,
 			asw_code,
 			asw_writer,
 			asw_content,
 			asw_date,
 			(SELECT count(*)
 			FROM qna_comment
 			WHERE qnano = #{qnaNo})commentCnt
 		FROM qna
 		WHERE qnano = #{qnaNo}
 	</select>
 	<insert id="write" parameterType="winwin.dto.QnaBoard">
 		INSERT INTO qna(qnano,title,userid,writer,pw,content,hit,writedate)
 		VALUES (qna_seq.nextval,#{title},#{userId},#{writer},#{pw},#{content},default,default)	
 	</insert>
 	<delete id="deleteBoard" parameterType="winwin.dto.QnaBoard">
		DELETE FROM qna
		WHERE qnano = #{qnaNo} 		
 	</delete>
 </mapper>